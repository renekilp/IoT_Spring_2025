#include <stdio.h>
#include <math.h>

// 8 # lecture materials vvv

int main(){

    double A = sqrt(9);
    double B = pow(2, 4);
    int C = round(3.14);    // %d *
    int D = ceil(3.14);     // always rounds up
    int E = floor(3.99);    // always rounds down
    double F = fabs(-100);  // absolute number
    double G = log(3);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

    printf("\n%lf", J);


    return 0;
}